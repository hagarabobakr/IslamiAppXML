<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.IslamiAppXML" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gold</item>
        <item name="colorPrimaryVariant">@color/gold_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gold</item>
        <item name="colorSecondaryVariant">@color/gold_dark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="background_img">
        <item name="android:background">@drawable/default_bg</item>
    </style>
    <style name="text_app_color">
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="icColor">
        <item name="android:tint">@color/gold</item>
    </style>
    <style name="cardBackgroundColor">
        <item name="cardBackgroundColor">@color/white</item>
    </style>
</resources>